% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{fitted.sbert_topic_sweep}
\alias{fitted.sbert_topic_sweep}
\title{Extract One Fitted Model from a Topic-Count Sweep}
\usage{
\method{fitted}{sbert_topic_sweep}(object, n_topics, ...)
}
\arguments{
\item{object}{A sweep returned by [select_topics()] with
`keep_models = TRUE` (the default).}

\item{n_topics}{The topic count to extract. Must be one of the candidates
in the sweep.}

\item{...}{Unused, present for generic consistency.}
}
\value{
The `sbert_topic_model` fitted for that candidate.
}
\description{
Returns the model [select_topics()] already fitted for a given topic
count, so choosing a granularity from the comparison table costs no
refitting.
}
\examples{
text <- c(
  "Cats chase mice", "Kittens chase mice too", "Cats nap daily",
  "Stocks and bonds trade", "Markets price shares", "Banks report profit"
)
embeddings <- rbind(
  c(1, 0, 0), c(0.98, 0.02, 0), c(0.96, 0, 0.04),
  c(0, 1, 0), c(0.02, 0.98, 0), c(0, 0.96, 0.04)
)
sweep <- select_topics(
  text, n_topics = 2:3, embeddings = embeddings, n_terms = 3
)
fitted(sweep, n_topics = 2)
}
