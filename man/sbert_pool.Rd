% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encode.R
\name{sbert_pool}
\alias{sbert_pool}
\title{Pool Token Embeddings into Sentence Embeddings}
\usage{
sbert_pool(
  token_embeddings,
  attention_mask,
  normalize = TRUE,
  method = c("mean", "cls")
)
}
\arguments{
\item{token_embeddings}{Numeric array shaped batch by sequence by hidden.}

\item{attention_mask}{Numeric matrix shaped batch by sequence, containing
only zero and one.}

\item{normalize}{Whether to L2-normalize every sentence vector.}

\item{method}{`"mean"` (default) or `"cls"` (first token).}
}
\value{
A numeric matrix shaped batch by hidden.
}
\description{
Applies the pooling strategy of the loaded model — attention-mask-aware
mean pooling (the Sentence-BERT default) or CLS-token pooling (used by the
BGE and mxbai families) — and optional row-wise L2 normalization.
}
\examples{
hidden <- array(1:12, dim = c(2, 3, 2))
mask <- matrix(c(1, 1, 0, 1, 0, 0), nrow = 2, byrow = TRUE)
sbert_pool(hidden, mask)
sbert_pool(hidden, mask, method = "cls")
}
